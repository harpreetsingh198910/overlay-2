# OBS Overlay Website

Upload the contents of this folder to the root of a GitHub Pages repository.

The site opens from index.html. Use the editor to save an overlay URL, then paste that URL into an OBS Browser Source. The generated link opens a clean overlay-only view with no editor controls.
